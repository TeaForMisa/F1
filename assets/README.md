# assets/

Положи сюда файл `welcome.jpg` — это фото будет прикрепляться к приветственному
сообщению после `/start` (handlers/common.py, WELCOME_PHOTO).

Если файла нет, бот не упадёт — просто отправит приветствие обычным текстом
без фото.
